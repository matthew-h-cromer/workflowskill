[project]
name = "workflowskill"
version = "0.8.1"
description = "Workflow engine for agent automation"
requires-python = ">=3.12"
dependencies = [
    "pyyaml>=6.0",
    "httpx>=0.28.0",
    "click>=8.1.0",
    "rich>=13.0.0",
    "python-dotenv>=1.0.0",
    "dbos>=2.0.0",
]

[project.optional-dependencies]
dev = [
    "anthropic>=0.43.0",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.25.0",
    "mypy>=1.13.0",
    "ruff>=0.9.0",
    "types-pyyaml>=6.0",
]

[project.scripts]
workflowskill = "workflowskill.main:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["cli/workflowskill"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["cli/tests", "evals"]
markers = [
    "eval: LLM-based eval tests (run with: uv run pytest -m eval)",
]

[tool.mypy]
python_version = "3.12"
strict = true
mypy_path = "cli"
exclude = ["cli/tests", "evals"]

[tool.ruff]
line-length = 100
src = ["cli"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
